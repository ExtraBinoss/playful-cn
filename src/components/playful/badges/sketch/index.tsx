import type { PlayfulBadgeProps } from '../badge-base'
import { BadgeBase } from '../badge-base'
export function SketchBadge(props: PlayfulBadgeProps) { return <BadgeBase variation="sketch" {...props} /> }
