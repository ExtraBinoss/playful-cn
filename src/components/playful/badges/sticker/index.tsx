import type { PlayfulBadgeProps } from '../badge-base'
import { BadgeBase } from '../badge-base'
export function StickerBadge(props: PlayfulBadgeProps) { return <BadgeBase variation="sticker" {...props} /> }
